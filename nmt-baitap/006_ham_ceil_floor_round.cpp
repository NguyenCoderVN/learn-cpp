#include <cmath>
#include <iostream>

using namespace std;

int main() {
  float X;
  cin >> X;

  cout << (int)floor(X) << endl;
  cout << (int)ceil(X) << endl;
  cout << (int)round(X) << endl;

  return 0;
}
